# Carillon - Firmware for individual bells

Uses ATtiny402 chip.


# Setup

Download and extract `sources.txt` into `toolchain/`

_the avr pack goes into `toolchain/packs` directory_



# Building / flashing

```
cd src
make
./flash
```
